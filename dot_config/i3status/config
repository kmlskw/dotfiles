general {
        colors = false
        interval = 1
}

order += "battery all"
order += "load"
order += "tztime local"

battery all {
        format = "%status %percentage %remaining"
}

tztime local {
        format = "%d-%m-%Y | %H:%M:%S"
}

load {
        format = "%1min"
}
